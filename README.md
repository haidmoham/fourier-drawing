# Fourier Drawing

A minimal canvas experiment for capturing a freehand path. Fourier analysis is intentionally out of scope for this first scaffold.

## Run locally

```sh
npm install
npm run dev
```

Run the data-handling tests with `npm test` and create a production build with `npm run build`.

